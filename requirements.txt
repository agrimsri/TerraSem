# TerraSem — Python dependencies (for pip-only installs without conda)
# Install PyTorch separately per https://pytorch.org/get-started/locally/
# e.g.: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

transformers>=4.40
timm
numpy<2.0
opencv-python-headless
open3d
pyyaml
tqdm
matplotlib
pandas
scikit-learn
onnx
onnxruntime
gradio
plotly
pytest
ruff
